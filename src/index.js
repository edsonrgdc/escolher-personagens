import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import EscolhePersonagem from './telas/EscolhePersonagem/EscolhePersonagem'
ReactDOM.render(
  <React.StrictMode>
  <EscolhePersonagem edinho />
  </React.StrictMode>,
  document.getElementById('root')
);
